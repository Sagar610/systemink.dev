services:
  # Backend API Service
  - type: web
    name: systemink-api
    env: node
    rootDir: apps/api
    buildCommand: cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @systemink/api build
    startCommand: cd ../.. && node apps/api/dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    plan: free

  # Frontend Static Site
  - type: web
    name: systemink-web
    env: static
    rootDir: apps/web
    buildCommand: cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @systemink/web build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://systemink-api.onrender.com/api
        sync: false
    plan: free

databases:
  - name: systemink-db
    plan: free
